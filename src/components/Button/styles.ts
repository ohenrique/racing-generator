import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    buttonContainer: {
        borderRadius: 8,
        padding: 16,
        backgroundColor: "#eb3636",
    },
    buttonText: {
        color: "#ffffff",
    },
});
